dependencies {
    implementation project(':core')
}

sourceSets {
    main {
        resources {
            srcDirs += [
                    project(':core').sourceSets.main.resources
            ]
        }
    }
}

bootJar { enabled = true }
jar { enabled = false }